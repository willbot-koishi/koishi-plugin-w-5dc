{
  "name": "koishi-plugin-w-5dc-web",
  "type": "module",
  "private": true,
  "packageManager": "yarn@4.4.0",
  "dependencies": {
    "5d-chess-js": "workspace:*",
    "5d-chess-renderer": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && mkdir -p ../lib/web && vite build && cp -r dist ../lib/web"
  }
}
